[build]
# Build command: Flutter builds to build/web
command = "flutter build web --release"
# Directory to publish (output from flutter build web)
publish = "build/web"

# Redirects for SPA routing (all requests go to index.html)
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
